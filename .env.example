# Optional overrides for `docker compose`. Copy to `.env` in the repo root.
# These values are local development defaults, not production secrets.

POSTGRES_USER=biowiki
POSTGRES_PASSWORD=biowiki
POSTGRES_DB=biowiki

CORS_ORIGINS=http://localhost:3000
BIOWIKI_ENV=development

NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_SITE_URL=http://localhost:3000
