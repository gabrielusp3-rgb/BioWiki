[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
pythonpath = .
markers =
    live: needs the populated BioWiki database and a running API at 127.0.0.1:8000
filterwarnings =
    ignore::DeprecationWarning
